[pytest]
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    # The app's own deprecations are tracked separately; keep test output readable.
    ignore::DeprecationWarning
    ignore::sqlalchemy.exc.LegacyAPIWarning
